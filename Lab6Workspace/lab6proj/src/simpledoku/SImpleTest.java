package simpledoku;
import java.lang.Math;

public class SImpleTest {
	
	public static void main(String[] agrs) {
		for (int x = 1; x <= 10; x++) {
			System.out.println(x + ", " + Math.pow(x, x) + ", " + (Math.pow(x, x)/(3E16)) + " years.");
		}
	}

}
